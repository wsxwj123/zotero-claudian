hello-section-header = Claude
    .tooltiptext = zotero-claudian chat
hello-section-sidenav =
    .tooltiptext = zotero-claudian chat
chrome-register-failed = zotero-claudian: chrome registration failed, the UI cannot load. Check Help → Debug Output Logging for details.
main-tab-button =
    .tooltiptext = Show/hide the Claude panel
prefs-pane-label = zotero-claudian
prefs-pane-title = Claude workbench
prefs-pane-intro = Settings for the Claude Code workbench: the AI's working directory, the default permission mode for new sessions, and the path to the claude executable. Changes apply immediately; running conversations are not interrupted.
prefs-workspace-label = Workspace directory
prefs-workspace-browse = Browse…
prefs-workspace-hint-empty = Leave empty to use the default: { $path }
prefs-workspace-hint-ok = Directory is ready (AI file output lands here)
prefs-workspace-hint-missing = Directory does not exist yet; it will be created on the next turn
prefs-workspace-hint-relative = Must be an absolute path (e.g. /Users/… or C:\…); a relative path cannot be used as the AI working directory
prefs-ws-mode-label = Workspace mode
prefs-ws-mode-item-single = Single directory — one workspace shared by all chats (default)
prefs-ws-mode-item-collection = Per collection — one subdirectory per collection
prefs-ws-mode-hint = Per collection: while you read an item inside a collection, the working directory switches to <workspace>/<collection name>, so you can drop a collection-specific project CLAUDE.md there (e.g. "answer in Chinese", "always cite page numbers"). The root CLAUDE.md still applies (Claude Code loads CLAUDE.md from the working directory upwards), so put general rules at the root and collection rules in the subdirectory.
prefs-mode-label = Default permission mode for new sessions
prefs-mode-hint = default = confirm each sensitive action; acceptEdits = allow file edits by default (recommended); plan = read-only planning; bypass = bypass all permission prompts (trusted tasks only)
prefs-mode-item-default = default — confirm each step
prefs-mode-item-acceptEdits = acceptEdits — allow file edits (recommended)
prefs-mode-item-plan = plan — read-only planning
prefs-mode-item-bypass = bypass — no permission prompts
prefs-mode-bypass-hint = Bypass mode: commands and file writes run without any permission prompt. The attachment-folder deny rules still block Write/Edit and shell redirection, but scripts (e.g. python) writing into that folder are not covered. Switching the chat header to this mode requires a second confirming click.
prefs-autoshow-label = Show the Claude pane automatically when opening a document
prefs-autoshow-hint = Switch to the sidebar Claude pane as soon as a PDF is opened; when off, click the sidebar icon yourself
prefs-cli-label = Path to the claude executable
prefs-cli-hint-empty = Leave empty to auto-detect (PATH and common install locations)
prefs-cli-hint-ok = Path is valid
prefs-cli-hint-missing = Path does not exist: falling back to auto-detection; a notice will appear at the top of the chat page
prefs-cli-hint-suspicious = The path exists, but the file is unusually small (possibly an incomplete download or quarantined by security software). Reinstall, or point to another claude install.
prefs-usage-label = Show token usage / balance
prefs-usage-hint = Show token usage and cache hit rate in the chat header; the DeepSeek account balance also shows when the provider is DeepSeek and a Key is set. Turned off = the balance is neither queried nor shown (no request to api.deepseek.com)
prefs-deepseek-label = DeepSeek API Key
prefs-deepseek-hint-empty = Leave empty to skip balance lookups. Used only to query the balance at api.deepseek.com (stored in plain text in Zotero settings)
prefs-deepseek-hint-set = Key set: the balance will show in the chat header (failures report a reason, never the key itself)
